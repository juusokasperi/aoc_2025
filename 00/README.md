## Solution 00

o build and run the file (works on Linux x86_64):
`nasm -f elf64 part1.asm -o part1.o && ld part1.o -o part1 && ./part1 input`
`nasm -f elf64 part2.asm -o part2.o && ld part2.o -o part2 && ./part2 input`
